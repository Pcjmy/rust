// 创建变量：let 关键字
// 变量默认是不可变的

fn main() {
    let x = 5;
    println!("The value of x is {}", x);

    // cannot assign twice to immutable variable
    // x = 6;
    // println!("The value of x is {}", x);
}
